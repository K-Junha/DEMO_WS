# Work Log — 2026-05-13

## Session: Demo Day App — 조성 입력 UI 개선

### 조성 드롭다운 입력 UI (11:38)
- **ExperimentDesignModal.vue**: 텍스트 직접 입력 → 산화물 드롭다운 + wt% 행 방식으로 전면 교체
  - SiO2 / Al2O3 / B2O3 3종 선택 가능
  - 행 추가/삭제 기능, 이미 선택된 산화물은 드롭다운에서 제외 (중복 방지)
  - wt% 실시간 합계 표시, 100% ± 0.1% 미충족 시 확인 버튼 비활성화
  - 확인 시 `"SiO2:70, Al2O3:18, B2O3:12"` 형태 문자열로 자동 변환
  - 모달 취소 시 form 초기화 추가 (defaultForm 함수 분리)
- pnpm build 성공, GitHub + HF 양쪽 push 완료

### 장치 카드 아이콘 크기 조정 (11:41)
- **DeviceCard.vue**: 헤더 우측 아이콘 `15px` → `24px`

### 실험 팝업 + DeviceCard shimmer 로딩 (12:36)
- **ExperimentBanner.vue**: 인라인 스테퍼 제거 → q-dialog 팝업으로 교체
  - `[장치명] 실험 중` 텍스트 (currentPhase 기반 실시간 업데이트)
  - 보고서 페이지 4장이 아래→위 슬라이드하는 "촤락" CSS 애니메이션 (pageFlip keyframe)
  - 진행률 바, 완료 시 1.6초 후 자동 닫힘
- **DevicePanel.vue**: `isStepReached` 개선 (phase='running' 장치는 false 반환)
  - `isDeviceLoading()` 함수 추가 → 현재 실험 중인 장치 판별
  - DeviceCard에 `:loading` prop 전달
- **DeviceCard.vue**: `loading` prop 추가
  - 실험 중인 장치: shimmer 스켈레톤 애니메이션 표시 (기존 데이터 사라짐)
  - 완료 후: 실제 데이터로 교체
- pnpm build 성공, GitHub + HF 양쪽 push 완료

### 수렴 시나리오 + 튜닝 팝업 + 자동 ID + TAS raw 트렌드
- **config/demo_data.yaml**: 전면 재설계 — 수렴 시나리오 (S-001~S-010)
  - 목표치: Tg=580°C, CTE=7.2, 유전율=5.8, 유전상수=2.6
  - S-001: 측정값이 목표에서 멀리 떨어짐 (TAS≈0.45, LIS≈0.85)
  - S-010: 측정값이 목표에 근접 (TAS≈0.996, LIS≈0.999)
  - S-011~S-020: 최적 근처 유지 (데모 중 여러 번 실험해도 Trend가 고점 유지)
- **ExperimentDesignModal.vue**: sampleId 입력 필드 제거 (ID 자동 생성)
- **experiment.ts**: `design()`·`addAutoTuneRow()`에서 S-001, S-002... 형식 자동 생성
- **CompositionTable.vue**: 튜닝 팝업 추가
  - 튜닝 버튼 클릭 → 분석 중 DNA-wave 애니메이션 (1.8초)
  - 이후 YAML `next_composition` 기반 추천 조성 표시
  - 확인 클릭 → 행 추가, 취소 가능
- **ScoreTrend.vue**: TAS 트렌드를 누적합 → 회차별 원본값(tasHistory)으로 변경
- **DeviceCard.vue**: 카드 너비 195→240px, 아이콘 24→30px
- pnpm build 성공, GitHub + HF 양쪽 push 완료

### 장치 카드 너비 소폭 축소 (16:02)
- **DeviceCard.vue**: 200~240px → 175~210px (두 줄 방지)

### BO 수렴 시나리오 전면 재설계 + 예측 버튼 로직 변경 (16:36)
- **config/demo_data.yaml**: 시나리오 완전 재작성
  - S-001~003: pred≈목표(모델 낙관), 실측 멀리 → LIS/TAS 둘 다 낮음
  - S-004: 모델 캘리브레이션 돌파 → LIS 0.65→0.91 급등
  - S-005: LIS↑(0.96) TAS↓(0.57) — 모델은 정확해졌지만 그 조성 성능 미달
  - S-008: LIS 유지, TAS 소폭 하락 (0.86→0.83)
  - S-010: 수렴 완료 (LIS=0.997, TAS=0.993)
  - 전 항목 소수점 변동값 적용 (상수 제거)
- **ExperimentDesignModal.vue**: 예측 버튼 로직 변경
  - 기존: 목표치 ±2.5% 랜덤 생성 → LIS≈TAS (의미 퇴색)
  - 변경: YAML linkedSample.predicted 값 사용 → LIS/TAS 독립 변동 가능
- pnpm build 성공, GitHub + HF 양쪽 push 완료

### 비단조 수렴 + 예측 버튼 + 카드 재축소 (16:19)
- **config/demo_data.yaml**: S-003·S-005·S-008 측정값을 하락 구간으로 조정
  - TAS 패턴: 0.45→0.57→0.52→0.65→0.63→0.78→0.87→0.85→0.94→0.996
  - 경향성은 상승하되 중간에 자연스러운 하락 구간 삽입
- **ExperimentDesignModal.vue**: YAML 미리보기 제거, AI 조성 예측 버튼 추가
  - wt% 합계 100% 충족 시 "조성 예측" 버튼 활성화
  - 클릭 → 1초 로딩 → 목표치 ±2.5% 범위 예측값 표시
  - 예측 완료 후 "확인" 버튼 활성화 (조성 변경 시 예측 초기화)
- **DeviceCard.vue**: 175~210px → 158~183px (추가 축소)
- pnpm build 성공, GitHub + HF 양쪽 push 완료

### 조성 소수점 + ScoreTrend 한글 타이틀 (16:53)
- **config/demo_data.yaml**: S-001~010 composition 문자열 소수점 전환
  - SiO2/Al2O3/B2O3 각 샘플이 연관성 있게 변화하며 합계 항상 100.0
  - next_composition 필드도 다음 샘플 조성과 일치하도록 업데이트
  - S-011~020 수렴 이후 구간도 소수점 적용
- **ScoreTrend.vue**: 차트 제목 영문 → 한글 변경
  - LIS Trend → 예측 정확도 추이 (LIS)
  - TAS Trend → 목표 달성도 추이 (TAS)
  - Y축 레이블 추가: LIS / TAS (X축 Num of Samples 방식과 동일하게)
- pnpm build 성공, GitHub + HF 양쪽 push 완료

### ScoreTrend 차트 제목 + 약자 풀네임 표시 (18:43)
- **ScoreTrend.vue**: 차트 제목 변경 및 약자 풀네임 副제목 추가
  - LIS 차트: '예측 정확도 추이' → '예측 모델 영향력 추이' + 副제목 'LIS: Learning Impact Score'
  - TAS 차트: 副제목 'TAS: Target Achievement Score' 추가
  - Plotly 타이틀 `<br>` + `<span>` 태그로 2줄 표시
- pnpm build 성공, GitHub + HF 양쪽 push 완료
