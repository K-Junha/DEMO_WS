# Work Log — 2026-05-12

## Session: Demo Day App — Initial Build

### 완료 작업

**Phase 1 — Scaffold (Task 1-1)**
- `frontend/` Quasar v2 + Vue 3 + TypeScript 프로젝트 수동 구성 (`@quasar/app-vite@2.6.0`)
- `backend/` FastAPI skeleton + pyproject.toml 구성
- `config/demo_data.yaml` 완전한 더미 데이터 작성 (S-001~S-020, 장치 9개)
- `.gitignore`, `Dockerfile` (multi-stage) 작성
- pnpm install 완료, Python miniconda 백엔드 의존성 설치 완료

**Phase 2 — Data Layer (Tasks 2-1, 2-2, 2-3)**
- YAML 설정파일 S-001~S-020 전체 작성
- `/api/config` 엔드포인트 (mtime hot-reload + ETag 304 캐싱)
- `/health` 엔드포인트
- FastAPI lifespan으로 `frontend/dist/spa` StaticFiles 마운트
- Pinia 3개 스토어: `useConfigStore`, `useExperimentStore`, `useTrendStore`
- `useExperimentStore`: `idle→designed→running→done` 상태머신, runId RESET 안전장치

**Phase 3 — CompositionTable (Tasks 3-1, 3-2)**
- `src/utils/scores.ts`: `calcLIS()`, `calcTAS()` 순수 함수
- `CompositionTable.vue`: 멀티 헤더, LIS(#d1fae5)/TAS(#ffedd5) 컬럼 그룹, 목표치 하단 행
- `ExperimentDesignModal.vue`: 조성명 + 예측값 4개 입력 폼

**Phase 4 — DevicePanel + Banner (Tasks 4-1, 4-2)**
- `DeviceCard.vue`: LED 애니메이션, 실험 중 데이터 표시
- `DevicePanel.vue`: `device_activation_interval_ms` 간격 순차 점등, runId 방식 stale-timeout 방지
- `ExperimentBanner.vue`: 9스텝 순차 running/complete 배너, 실험 완료 시 측정값 자동 입력, LIS/TAS 계산, Score Trend 누적, auto_tune 다음 행 추가

**Phase 5 — ScoreTrend (Task 5-1)**
- `ScoreTrend.vue`: `plotly.js-basic-dist` lazy import, LIS(파랑)/TAS(주황) 누적합 꺾은선 그래프

**Phase 6 — Integration (Task 6-1)**
- `App.vue`: 3행 레이아웃 (DevicePanel / CompositionTable / ScoreTrend) 조립

### 검증 결과
- `pnpm build` 성공 (SPA 빌드, Plotly 1044KB chunk lazy-loaded)
- 백엔드 pytest 9/9 통과 (health, config shape, ETag 304, YAML 파싱)
- `/health` 200, `/api/config` 200, SPA fallback 정상 서빙 확인

### 기능 수정 (17:35)
- **Device 카드 데이터**: 초기 연결 시 S-001 기본 데이터 표시 제거 → 실험 진행 중 해당 스텝 도달 시에만 표시
- **실험 배너 → 9단계 스테퍼**: 완료(초록)/진행중(인디고 스피너)/대기(반투명) + 퍼센트 프로그레스 바
- **튜닝 수정**: ExperimentDesignModal에서 `sourceId`를 YAML 샘플 순서에 맞게 자동 연결하여 튜닝/측정값 정상 동작
- **Device 아이콘**: balance/rotate_right/whatshot/thermostat/vertical_align_center/show_chart/analytics/straighten/bolt

### UI 전면 개선 (17:26) — demoday_streamlit 테마 적용
- **다크 테마**: `#0f172b`/`#1d293d`/`#315158` 팔레트, Streamlit config.toml 색상 동일 적용
- **RESET 버튼**: CompositionTable → 헤더 툴바 우측으로 이동
- **Device 카드**: 활성화 즉시 S-001 데모 데이터 표시 (실험 전에도 데이터 가시화)
- **프로토콜 배지**: RS-232(앰버)/RS-485(파랑)/Ethernet(초록) 색상 코딩
- **글로벌 목표치**: tfoot 행 제거 → 별도 그라디언트 카드 (Tg/CTE/유전율/유전상수 크게 표시)
- **조성 테이블**: 패딩 확대, 측정값 초록색, 대문자 헤더, 빈 상태 안내문 추가
- **Score Trend**: 다크 Plotly 레이아웃, fill-to-zero 영역, LIS=파랑/TAS=초록 (Streamlit Altair 색상 동일)
- **quasar.config.ts**: `dark: true` 추가

### 버그 수정 (16:42)
- **Pinia "no active Pinia" 오류**: `src/boot/pinia.ts` 생성 누락으로 앱 초기화 실패
  - `src/boot/pinia.ts` 추가 (`app.use(createPinia())`)
  - `quasar.config.ts` boot 배열에 `'pinia'` 추가 (`['pinia', 'axios']`)

### Device 카드 UI 개선 (컨텍스트 이어받기)
- **DeviceCard.vue**: 아이콘 헤더 우측 정렬 (`[LED + 이름] [space] [아이콘]`)
- **DeviceCard.vue**: 활성화된 카드는 데이터 없어도 구조 항상 표시 (빈 값은 `—`)
- **DeviceCard.vue**: DTA/DSC → `Tg: {값} °C`, 딜라토미터 → `CTE: {값} ppm`, 유전율측정기 → `유전율 / 유전상수` 측정값 표시
- **DevicePanel.vue**: `measurementFor()` 추가 → `sample.measurement` 패스스루
- **config.ts**: `SampleMeasurement` 인터페이스 분리 export
- `pnpm build` 성공 확인

### 버그 수정 2차 (19:09)
- **DevicePanel.vue**: 실험 완료 후 auto-tune이 state='designed'로 바꿔도 장치 데이터 유지 (`isStepReached` + `getSample` 개선)
- **ExperimentBanner.vue**: `finishExperiment`의 auto_tune 자동 행 추가 제거 → 튜닝 버튼 단독 사용, 중복 행 방지
- **ExperimentDesignModal.vue**: 공통 목표치(Tg/CTE/유전율/유전상수) 입력 필드 추가, 열릴 때 현재값 초기화, 확인 시 `configStore.updateGlobalTarget()` 호출
- **DeviceCard.vue**: 서냉로 "정지" 행 제거; 전기로 스텝별 승온(°/min)·목표(°C)·유지 3행 레이아웃으로 개선
- **config.ts**: `updateGlobalTarget()` 액션 추가
- `pnpm build` 성공 확인

### GitHub + HuggingFace 배포 준비 (19:xx)
- `README.md` 생성 (HF Space 메타데이터: sdk=docker, app_port=7860, title=WS Lab Pilot)
- `.dockerignore` 생성
- git init, remote → https://github.com/K-Junha/DEMO_WS
- 기존 원격 main → `bak` 브랜치로 백업
- 36개 파일 커밋 후 origin/main에 force push 완료

### HuggingFace 빌드 오류 수정 (19:36)
- **Dockerfile**: `node:20-alpine` → `node:22-alpine` (@quasar/app-vite Node 22+ 필요)
- HF Space + GitHub 양쪽 push 완료

### HuggingFace 빌드 오류 수정 2차 (19:38)
- **Dockerfile**: `pnpm@10` 버전 고정 (pnpm v11 빌드 스크립트 정책 변경 우회)
- **package.json**: engines에 `^22` 추가, `onlyBuiltDependencies`에 `@parcel/watcher` 추가

### 한글 주석 추가 (19:49)
- `scores.ts`: LIS/TAS 공식 수학적 의미 및 하한선 처리 설명
- `experiment.ts`: 상태 머신(idle→designed→running→done) 흐름, runId stale 방지 원리
- `trend.ts`: 누적합 용도 및 호출 주체 명시
- `config.ts`: 인터페이스↔YAML 필드 대응, updateGlobalTarget 동작 방식
- `ExperimentBanner.vue`: runId 방어 로직, 진행률 계산, finishExperiment 흐름
- `DevicePanel.vue`: isStepReached 판단 기준, getSample의 designed 예외 처리 이유
- `App.vue`: 3패널 구조, 버튼 활성화 조건, handleReset 동작
- pnpm build 성공, GitHub + HF 양쪽 push 완료

### 남은 작업
- HuggingFace 빌드 완료 확인 (https://huggingface.co/spaces/jhkim93/ws-lab-pilot)
